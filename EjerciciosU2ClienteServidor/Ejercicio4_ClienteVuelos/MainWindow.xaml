<Window x:Class="Ejercicio4_ClienteVuelos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ejercicio4_ClienteVuelos"
        mc:Ignorable="d"
        Title="Vuelos" Height="350" Width="500">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="15"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
        </Style>
    </Window.Resources>
    <StackPanel>
        <Label FontSize="30" HorizontalAlignment="Center">CONTROL DE VUELOS</Label>
        <Label>Hora de Salida:</Label>
        <TextBox Text="{Binding Hora}"></TextBox>
        <Label>Destino:</Label>
        <TextBox Text="{Binding Destino}"></TextBox>
        <Label>Vuelo:</Label>
        <TextBox Text="{Binding Vuelo}"></TextBox>
        <Label>Estado:</Label>
        <TextBox Text="{Binding Estado}"></TextBox>

        <Button Name="btnAgregar" Click="btnAgregar_Click" Content="Agregar Vuelo"/>
        <Button Name="btnEliminar" Click="btnEliminar_Click" Content="Eliminar Vuelo"/>
        <Button Name="btnEditar" Click="btnEditar_Click" Content="Editar Vuelo"/>
    </StackPanel>
</Window>
